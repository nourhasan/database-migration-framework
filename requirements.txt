# Python Database Migration Framework - Dependencies
# Install only the drivers you need for your target database

# ========================================
# Required Core Dependencies
# ========================================
python-dotenv>=1.0.0       # Environment configuration
openpyxl>=3.1.0            # Excel file reading

# ========================================
# Database Drivers (Install Based on DB_ENGINE)
# ========================================

# SQL Server
# Requires: ODBC Driver 17 for SQL Server (system-level installation)
# Install command: pip install pyodbc
pyodbc>=5.0.0

# PostgreSQL  
# Install command: pip install psycopg2-binary
# Note: Using flexible version to ensure Windows wheel availability
psycopg2-binary>=2.9.0

# MySQL
# Install command: pip install mysql-connector-python
mysql-connector-python>=8.0.0

# ========================================
# Optional: Install Only What You Need
# ========================================
# If you only use SQL Server:
#   pip install python-dotenv openpyxl pyodbc
#
# If you only use PostgreSQL:
#   pip install python-dotenv openpyxl psycopg2-binary
#
# If you only use MySQL:
#   pip install python-dotenv openpyxl mysql-connector-python
